/* в двумерном массиве информация о баллах 20 спорцменов, учавствовавших
в 5 видах спорта. Посчитать сумму баллов у любого спортсмена */
#include <stdio.h>
#include <malloc.h>
#include <stdlib.h>
#include <time.h>
#include <conio.h>
int main()
{
	srand(time(NULL));
	int **a;
	int i, j, n = 20, m = 5, sum = 0;
	a = (int**)malloc(m * sizeof(int*));
	for (i = 0; i<10; i++)
		printf("s%d  ", i);
	for (i = 10; i<n; i++)
		printf("s%d ", i);
	printf("\n");
	for (i = 0; i<m; i++) {
		a[i] = (int*)malloc(n * sizeof(int));
		for (j = 0; j < n; j++) {
			a[i][j] = 10 + rand() % 90;
			printf("%d  ", a[i][j]);
		}
		printf("\n");
	}
	printf("\nКакой спортсмен (0-19) вас интересует?\n");
	scanf("%d", &i);
	for (j = 0; j < m; j++)
		sum += a[j][i];
	printf("У спортсмена под номером %d сумма баллов %d.\n", i, sum);
	for (i = 0; i < m; i++)
		free(a[i]);
	free(a);
	getch();
	return 0;
}
